#include <iostream>
#include <vector>

#define FOR(i, a, b)                \
    for (int i = (a); i < (b); i++) \
        ;

using namespace std;

int peopleAwareOfSecret(int n, int delay, int forget)
{
    FOR(i, 0, n)
    {
    }
}

void solve()
{
    int N;
    cin >> N;
    vector<int> nums(N);
    for (int i = 0; i < N; i++)
    {
        cin >> nums[i];
    }
}

int main()
{

    int T = 1;
    // cin >> T;

    while (T--)
    {
        solve();
    }

    return 0;
}